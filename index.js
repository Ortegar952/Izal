// const p = document.getElementsByClassName('container')[0];
// p.onclick = function() {
// console.log(p)
//  // Trigger the `hover` event on the paragraph
//  p.onhover.call(p);
// };


$('body').on('touchstart', function() {
    console.log("Im alive")
});
